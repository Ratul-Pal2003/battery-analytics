# Netlify Configuration for Zenfinity Battery Analytics Dashboard

[build]
  # Build command
  command = "npm run build"

  # Directory to publish (Vite outputs to dist/)
  publish = "dist"

  # Node version
  node_version = "24.12.0"

# Proxy API requests to avoid CORS issues
[[redirects]]
  from = "/api/*"
  to = "https://zenfinity-intern-api-104290304048.europe-west1.run.app/api/:splat"
  status = 200
  force = true

# Redirect all other requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for better performance and security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
